D:\Microsoft VS Code\lidan\search_web\
├── app/                  # 【核心后端】
│   ├── main.py           # FastAPI 入口，定义了 API 路由和搜索接口
│   ├── database.py       # 数据库连接 (SQLAlchemy + SQLite)
│   ├── models.py         # 数据库表结构 (Inscription 表)
│   ├── crud.py           # 数据库操作 (实现了带权重的模糊搜索逻辑)
│   └── static/           # 【静态资源】
│       ├── images/       # 存放解析出的图片 (Docker 挂载点)
│       ├── css/
│       └── js/
├── data/                 # 【数据存储】
│   ├── raw_word/         # 存放原始 Word 文档 (待放入)
│   └── inscriptions.db   # SQLite 数据库文件 (自动生成)
├── scripts/              # 【工具脚本】
│   └── word_parser.py    # Word 解析脚本骨架 (待填充详细逻辑)
├── templates/            # 【前端模板】
│   └── index.html        # 首页模板 (待开发)
├── docker/               # 【部署配置】
│   └── Dockerfile        # 容器构建文件
├── docker-compose.yml    # 容器编排配置 (已配置好 Volume 挂载)
├── requirements.txt      # Python 依赖列表
├── .gitignore            # Git 忽略配置
└── README.md             # 项目说明文档